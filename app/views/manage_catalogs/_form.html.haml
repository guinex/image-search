= form_for @manage_catalog do |f|
  - if @manage_catalog.errors.any?
    #error_explanation
      %h2= "#{pluralize(@manage_catalog.errors.count, "error")} prohibited this manage_catalog from being saved:"
      %ul
        - @manage_catalog.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
